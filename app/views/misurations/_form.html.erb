<%= form_with(model: misuration, local: true) do |form| %>
  <% if misuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(misuration.errors.count, "error") %> prohibited this misuration from being saved:</h2>

      <ul>
        <% misuration.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mac %>
    <%= form.text_field :mac %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :unit %>
    <%= form.text_field :unit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
