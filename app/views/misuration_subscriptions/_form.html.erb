<%= form_with(model: misuration_subscription, local: true) do |form| %>
  <% if misuration_subscription.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(misuration_subscription.errors.count, "error") %> prohibited this misuration_subscription from being saved:</h2>

      <ul>
        <% misuration_subscription.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mac %>
    <%= form.text_field :mac %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
